1) Allow user to specify which types of source files they are interested in.
2) Allow user to specify time range
3) Calculate average lines of code added per commit, as well as average time in between commits
